---
type: todo
date: 2026-03-09
author: Antigravity
project: Scenario Plugin — Write-back MVP (SQLite)
rethink: 2026-03-09 — Pivoted back to SQLite MVP. Dropping PostgreSQL and dbt to unblock immediate development.
---

# Scenario Plugin — Task Breakdown

## Ground Truth

- **Design Doc:** Simplified to SQLite native integration
- **DB target:** `~/.superset/superset.db` (Table: `silver_scenario_equity_share`)

---

## Epic 1: Database Layer

### T1 — SQL Migration Script (SQLite)

- **File:** `scripts/init_db_script/create_scenario_table.sql`
- **Action:** Create the `silver_scenario_equity_share` table with UPSERT-friendly unique constraints for SQLite.
- **Done when:** `sqlite3 ~/.superset/superset.db < scripts/init_db_script/create_scenario_table.sql` runs without errors.
- **Risk:** Low
- **Status:** [x]

---

## Epic 2: Backend Flask API

### T3 — Rewrite `scenario_writeback.py` Blueprint for SQLite

- **File:** `superset/superset/views/scenario_writeback.py`
- **Action:** Replace the PG dialect stub with simple SQLite-compatible `ON CONFLICT DO UPDATE` raw text SQL execution using `superset.db.session`.
- **Done when:** Blueprint handles POST payloads and correctly UPSERTs into the `superset.db` SQLite file.
- **Risk:** Low
- **Status:** [x]

### T4 — Confirm Blueprint Registration in superset_config.py

- **File:** `superset_config.py`
- **Action:** The `EXTRA_BLUEPRINTS` pattern is already stubbed in `superset_config.py`. Ensure health check confirms registration once Superset boots.
- **Risk:** Low
- **Status:** [ ]

---

## Epic 3: React Plugin

### T5 — Scaffold Plugin Package

- **File:** `superset-frontend/plugins/plugin-chart-scenario/` (new directory)
- **Action:** Use the existing `plugin-chart-ag-grid-table` as a scaffold reference (same directory structure). Create `package.json`, `tsconfig.json`, `src/index.ts`, `src/ScenarioChart.tsx`, `src/plugin.ts`.
- **Done when:** `cd plugin-chart-scenario && npm run build` completes without TypeScript errors.
- **Risk:** Medium — monorepo build configuration
- **Status:** [x]

### T6 — Port HTML/JS Logic into React Component

- **File:** `superset-frontend/plugins/plugin-chart-scenario/src/ScenarioChart.tsx`
- **Action:** Port all logic from `docs/scenario-ui.html` into a React functional component:
  - `useState` for `EXISTING_ROWS` and `GROWTH_ROWS` arrays
  - Tabs, stat cards, AG Grid or plain HTML table for the grid
  - Chart.js or Echarts line chart (prefer Echarts — already bundled in Superset)
  - Optimistic `setGridData` on cell input, then `SupersetClient.post` in background
- **Done when:** Component renders in Storybook with no errors; chart moves when a cell is edited.
- **Risk:** Medium
- **Status:** [x]

### T7 — Register Plugin in MainPreset.ts

- **File:** `superset-frontend/src/visualizations/presets/MainPreset.ts`
- **Action:** Following the existing `AgGridTableChartPlugin` pattern:
  1. Import `ScenarioChartPlugin from '@superset-ui/plugin-chart-scenario'`
  2. Add `new ScenarioChartPlugin().configure({ key: VizType.Scenario })` to the `plugins` array
  3. Add `Scenario = 'scenario_chart'` to the `VizType` enum in `@superset-ui/core`
- **Done when:** Superset frontend builds; "Scenario" chart type appears in the chart gallery (`Create Chart` dropdown).
- **Risk:** Medium
- **Status:** [x]

### T8 — Wire Write-back in React Component

- **File:** `superset-frontend/plugins/plugin-chart-scenario/src/ScenarioChart.tsx`
- **Action:** Complete the `handleCellEdit` function using `SupersetClient.post`:

  ```typescript
  await SupersetClient.post({
    endpoint: '/api/v1/scenario/writeback',
    jsonPayload: { scenario_name, bu, opu, year, value, scenario_id },
  });
  ```

- **Done when:** Edit a cell in the live Superset dashboard → Network tab shows a 200 POST.
- **Risk:** Low
- **Status:** [x]

---

## Dependency Order

```
T1 (SQLite DDL) → T3 (Flask API config) → T4 (Verify route)
                                                        ↓
T5 (scaffold plugin) → T6 (port logic) → T7 (register) → T8 (wire writeback)
```

> **Rethink note (2026-03-09):** T9, T10, T11 (dbt and downstream charts) dropped to unblock MVP local development on SQLite.
