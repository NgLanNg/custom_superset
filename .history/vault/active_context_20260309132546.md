# Active Context

## Current Objective

Install Apache Superset from scratch on macOS (using Python `pip` and a virtual environment) in the current workspace (`/Users/alan/dashboard`), abandoning the Podman/Docker approach.

## Current Phase

Phase 1: Discovery & Analysis (Initial setup)

## Key Decisions

- Adopted `dev` role (Full-Stack Data Developer).
- Abandoned Docker/Podman compose installation per user request ("no postman please" -> "no podman please").
- Will install Python 3.11 via Homebrew (since Superset is well supported on 3.10 and 3.11, but not 3.12+).
- Will create a Python virtual environment and use `pip install apache-superset`.

## Next Steps

1. Install OS dependencies via Homebrew (Python 3.11, openssl, sqlite, pkg-config).
2. Create and activate a Python virtual environment.
3. Install Apache Superset and configure basic settings.
4. Execute DB initialization and create an admin user.
