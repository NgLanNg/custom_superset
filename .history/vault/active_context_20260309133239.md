# Active Context

## Current Objective

Set up Apache Superset manually from source for local development (Open Source Developer mode) in the `/Users/alan/dashboard/superset` directory.

## Current Phase

Phase 1: Discovery & Analysis (Initial setup - Dev Mode pivot)

## Key Decisions

- Adopted `dev` role.
- Abandoned both Docker and basic `pip install apache-superset` approaches.
- Pivoting to the full developer workflow: cloning the source code, installing python backend in editable mode (`pip install -e .`), and building/running the React frontend via npm.
- Will use Python 3.11 (already installed via Homebrew) and `npm` (Node.js).

## Next Steps

1. Clone the Apache Superset repository.
2. Set up Python virtual environment and install backend dependencies in editable mode (`pip install -e .`).
3. Set up frontend dependencies (`npm install`) and build assets.
4. Initialize the database and create a test admin user.
5. Create scripts to run backend and frontend dev servers.
