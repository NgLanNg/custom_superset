name: 'scenario_analytics'
version: '1.0.0'
config-version: 2

profile: 'superset_postgres'

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  scenario_analytics:
    staging:
      +materialized: view
    gold:
      +materialized: table
